#ifndef DETACH_EXPR_AFTER_BODY_STAGE_HPP
#define DETACH_EXPR_AFTER_BODY_STAGE_HPP

struct detach_expr_after_body_stage {
    auto operator<=>(const detach_expr_after_body_stage&) const = default;
};

#endif
